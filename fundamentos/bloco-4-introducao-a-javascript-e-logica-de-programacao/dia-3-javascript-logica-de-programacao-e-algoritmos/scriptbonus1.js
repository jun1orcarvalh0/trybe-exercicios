let n = 5;
if (n >= 1); {
  for(let index = 1; index <= n; index += 1) {
    let square = "";
    for(let isquare = 1; isquare <= n; isquare += 1) {
    square += "*"
    }
    console.log(square);
  }
}